FROM nginx:alpine

# Copy the pre-built frontend files
COPY frontend/dist /usr/share/nginx/html

# Copy nginx configuration from prod root directory
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]